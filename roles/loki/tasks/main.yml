---
- name: Deploy Loki
  when: ansible_hostname == hostvars[groups[group_name_master | default('master')][0]]['ansible_hostname']
  run_once: true
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml
  block:
    - name: Create tmp directory
      ansible.builtin.file:
        path: /tmp/k3s
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Create monitoring namespace
      ansible.builtin.command: >-
        {{ k3s_kubectl_binary | default('k3s kubectl') }} create namespace {{ monitoring_namespace }}
      register: monitoring_ns
      failed_when: monitoring_ns.rc != 0 and 'already exists' not in monitoring_ns.stderr
      changed_when: monitoring_ns.rc == 0

    - name: Add Grafana Helm repo
      ansible.builtin.command: helm repo add grafana https://grafana.github.io/helm-charts
      register: helm_repo
      failed_when: helm_repo.rc != 0 and 'already exists' not in helm_repo.stderr
      changed_when: helm_repo.rc == 0

    - name: Update Helm repos
      ansible.builtin.command: helm repo update
      changed_when: false

    - name: Template Loki Helm values
      ansible.builtin.template:
        src: loki-values.yaml.j2
        dest: /tmp/k3s/loki-values.yaml
        owner: root
        group: root
        mode: "0644"

    - name: Install/Upgrade Loki via Helm
      ansible.builtin.command: >-
        helm upgrade --install {{ loki_release_name }} grafana/loki
        --namespace {{ monitoring_namespace }}
        --version {{ loki_chart_version }}
        -f /tmp/k3s/loki-values.yaml
        --wait
        --timeout 10m
      register: loki_install
      changed_when: "'has been upgraded' in loki_install.stdout or 'has been installed' in loki_install.stdout"

    - name: Wait for Loki deployment to be ready
      ansible.builtin.command: >-
        {{ k3s_kubectl_binary | default('k3s kubectl') }} wait deployment/loki
        --namespace={{ monitoring_namespace }}
        --for=condition=Available
        --timeout=120s
      register: loki_wait
      changed_when: false
      until: loki_wait is succeeded
      retries: 10
      delay: 10

    - name: Verify Loki pods
      ansible.builtin.command: >-
        {{ k3s_kubectl_binary | default('k3s kubectl') }} get pods -n {{ monitoring_namespace }} -l "app.kubernetes.io/name=loki"
      changed_when: false
